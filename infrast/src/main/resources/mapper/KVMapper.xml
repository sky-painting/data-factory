<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tianhua.datafactory.infrast.dao.mapper.KVMapper">
    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.tianhua.datafactory.infrast.dao.dataobject.KVPairDO">
        <result column="id" property="id" />
        <result column="k" property="key" />
        <result column="v" property="value" />
        <result column="value_json" property="valueJson" />
        <result column="group_key" property="groupKey" />
        <result column="parent_key" property="parentKey" />
        <result column="refer_key" property="referKey" />
        <result column="refer_id" property="referId" />
        <result column="value_type" property="valueType" />
        <result column="key_type" property="keyType" />
        <result column="status" property="status" />
    </resultMap>

    <!--查询总数-->
    <resultMap type="java.lang.Integer" id="count">
        <result column="total"/>
    </resultMap>


    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id,k,v,value_json,group_key,parent_key,refer_key,refer_id, key_type, value_type,status
    </sql>

    <insert id="insert" keyColumn="id" keyProperty="id" parameterType="com.tianhua.datafactory.infrast.dao.dataobject.KVPairDO" useGeneratedKeys="true">
        insert into kv_instance(
            k,v,value_json,group_key,parent_key,refer_key,refer_id,value_type,key_type,status)
        values(
        #{key},#{value},#{valueJson},#{groupKey},#{parentKey},#{referKey},#{referId},#{valueType},#{keyType},#{status}
        )
    </insert>

    <insert id="insertBatch">

        insert into kv_instance(k,v,value_json,group_key,parent_key,refer_key,refer_id,value_type,key_type,status)
        values
        <foreach collection="list" item="kv" separator=",">
            (#{kv.key},#{kv.value},#{kv.valueJson},#{kv.groupKey},#{kv.parentKey},#{kv.referKey},#{kv.referId},#{kv.valueType},#{kv.keyType},#{kv.status})
        </foreach>
    </insert>




    <update id="update" parameterType="com.tianhua.datafactory.infrast.dao.dataobject.KVPairDO">
        update kv_instance
        set v=#{value}
        <set>
            <if test="value != null">v=#{value},</if>
            <if test="valueJson != null">value_json=#{valueJson},</if>
            <if test="groupKey != null">group_key=#{groupKey},</if>
            <if test="parentKey != null">parent_key=#{parentKey},</if>
            <if test="referKey != null">refer_key=#{referKey},</if>
            <if test="referId != null">refer_id=#{referId},</if>
            <if test="valueType != null">value_type=#{valueType},</if>
            <if test="keyType != null">key_type=#{keyType},</if>
            <if test="status != null">status=#{status},</if>
        </set>
        where  k = #{key}
    </update>

    <select id="selectAll" resultMap="BaseResultMap">
        select <include refid="Base_Column_List" /> from kv_instance
    </select>

    <select id="selectOne" resultMap="BaseResultMap">
        select <include refid="Base_Column_List" /> from kv_instance
        <where>
            <if test="key != null and key != ''">
                AND k = #{key}
            </if>

            <if test="value != null and value != ''">
                AND v = #{value}
            </if>

            <if test="groupKey != null and groupKey != ''">
                AND group_key = #{groupKey}
            </if>

            <if test="parentKey != null and parentKey != ''">
                AND parent_key = #{parentKey}
            </if>

            <if test="referKey != null and referKey != ''">
                AND refer_key = #{referKey}
            </if>

            <if test="referId != null and referId != ''">
                AND refer_id = #{referKey}
            </if>

        </where>
        limit 1
    </select>


    <select id="count" resultType="java.lang.Integer">
        select count(1) from kv_instance
        <where>
            <if test="key != null and key != ''">
                AND k = #{key}
            </if>

            <if test="value != null and value != ''">
                AND v = #{value}
            </if>

            <if test="groupKey != null and groupKey != ''">
                AND group_key = #{groupKey}
            </if>

            <if test="parentKey != null and parentKey != ''">
                AND parent_key = #{parentKey}
            </if>

            <if test="referKey != null and referKey != ''">
                AND refer_key = #{referKey}
            </if>

            <if test="referId != null and referId != ''">
                AND refer_id = #{referKey}
            </if>

        </where>
        limit 1
    </select>



    <delete id="delete" >
        delete from kv_instance

        <where>
            <if test="key != null and key != ''">
                AND k = #{key}
            </if>

            <if test="value != null and value != ''">
                AND v = #{value}
            </if>

            <if test="groupKey != null and groupKey != ''">
                AND group_key = #{groupKey}
            </if>

            <if test="parentKey != null and parentKey != ''">
                AND parent_key = #{parentKey}
            </if>

            <if test="referKey != null and referKey != ''">
                AND refer_key = #{referKey}
            </if>

            <if test="referId != null and referId != ''">
                AND refer_id = #{referKey}
            </if>
        </where>
    </delete>

    <select id="selectList" resultMap="BaseResultMap">
        select <include refid="Base_Column_List" />  from kv_instance
        <where>
            <if test="key != null and key != ''">
                AND k = #{key}
            </if>

            <if test="value != null and value != ''">
                AND v = #{value}
            </if>

            <if test="groupKey != null and groupKey != ''">
                AND group_key = #{groupKey}
            </if>

            <if test="parentKey != null and parentKey != ''">
                AND parent_key = #{parentKey}
            </if>

            <if test="referKey != null and referKey != ''">
                AND refer_key = #{referKey}
            </if>

            <if test="referId != null and referId != ''">
                AND refer_id = #{referKey}
            </if>

            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </select>

</mapper>
